# ── LoomPipe Docker environment ───────────────────────────────────────────────
# Copy this file to .env and set your own values before running docker compose.
# NEVER commit .env to source control.

# SQL Server SA password (min 8 chars, upper+lower+digit+symbol)
DB_SA_PASSWORD=LoomPipe_Str0ng!

# JWT signing key — MUST be changed in production (minimum 64 characters)
JWT_SECRET_KEY=CHANGE_ME_IN_PRODUCTION_USE_A_LONG_RANDOM_64_CHAR_KEY!!

# Host port the app is exposed on
APP_PORT=8080
